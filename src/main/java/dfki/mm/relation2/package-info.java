/**
 * RelationFilter:
 *   full pbf -> filtered pbf
 *   keeps relations (route) and related ways/nodes
 * ConvertCSV:
 *   filtered pbf -> 5 csv
 *     bus stops
 *     rail stops
 *     line nodes
 *     bus lines
 *     rail lines
 */
package dfki.mm.relation2;